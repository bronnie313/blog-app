<%= form_with model: @comment, url: add_user_comment_path(user_id: @user.id, post_id: @post.id) do |form| %>
    <%= form.label :text %>
    <%= form.text_field :text %>
    <%= form.submit 'Add Comment' %>
<% end %>
  